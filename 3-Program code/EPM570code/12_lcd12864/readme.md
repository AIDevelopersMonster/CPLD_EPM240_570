# LCD12864 — вывод текста на дисплей с CPLD EPM240 / EPM570

Данный проект демонстрирует работу символьного графического дисплея **LCD12864**  
в текстовом режиме (HD44780-совместимом) с платой **EPM240 / EPM570 CPLD Development Board**.

Модуль `LCD12864.v` последовательно инициализирует дисплей и выводит на него четыре строки текста:


Our FPGA
EDA NIOS II
SOPC
FPGA A


---

## 🔧 Оборудование

- Плата: **EPM240 / EPM570 CPLD Development Board**
- Дисплей: **LCD12864** (в текстовом режиме, совместимом с HD44780)
- Тактовая частота: ~50 МГц

---

## 📂 Основной файл

### `LCD12864.v`

Функции модуля:

- инициализация LCD12864 (последовательность команд `0x30`, `0x0C`, `0x06`, `0x01`);
- работа конечного автомата (FSM) для пошаговой отправки команд и символов;
- генерация замедленного тактового сигнала `clkr` для LCD на основе входного `clk`;
- формирование сигналов:
  - `rs` — выбор регистра (0 — команда, 1 — данные),
  - `rw` — режим записи (всегда 0),
  - `en` — строб (Enable),
  - `dat[7:0]` — шина данных к дисплею.

На экране по строкам отображается:

1. `Our FPGA`
2. `EDA NIOS II`
3. `SOPC`
4. `FPGA A`

---

## 🔌 Подключение LCD12864 к CPLD

Общая идея подключения (номера выводов зависят от разводки твоей платы):

| Сигнал LCD | Назначение        | Подключение к CPLD         |
|------------|-------------------|----------------------------|
| RS         | Регистр / данные  | Любой свободный вывод GPIO |
| RW         | Чтение/запись     | GND (работаем только на запись) |
| EN         | Строб (Enable)    | Любой свободный вывод GPIO |
| D0–D7      | Шина данных       | 8 линий GPIO               |
| VSS        | GND               | Земля                      |
| VDD        | +5V               | Питание дисплея            |
| V0         | Контраст          | Через потенциометр на GND/VDD |
| LED+ / A   | Подсветка (+)     | +5V через резистор         |
| LED- / K   | Подсветка (−)     | GND                        |

> Важно: CPLD работает на 3.3 В, но входы LCD12864 нормально воспринимают 3.3 В как логическую «1»,  
> а `RW` заземлён, поэтому обратного чтения с дисплея нет — это безопасная схема.

---

## ⚙️ Принцип работы

1. Счётчик `counter` делит частоту входного `clk` и формирует более медленный тактовый сигнал `clkr` для автомата.
2. В `always @(posedge clkr)` работает конечный автомат:
   - Состояния `set0`–`set3` — отправка команд инициализации дисплея.
   - Далее состояния `dat0`, `dat1`, ... — по одному символу отправляют ASCII-коды букв для строк:
     - `Our FPGA`
     - `EDA NIOS II`
     - `SOPC`
     - `FPGA A`
3. Состояние `nul` завершает цикл и может удерживать отображение либо повторять вывод (в исходном примере — несколько проходов).

---

## 🔗 Полезные ссылки

Репозиторий проекта:  
https://github.com/AIDevelopersMonster/CPLD_EPM240_570

Плейлист FPGA / CPLD (YouTube):  
https://www.youtube.com/playlist?list=PLVoFIRfTAAI7-d_Yk6bNVnj4atUdMxvT5




