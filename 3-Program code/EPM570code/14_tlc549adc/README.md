# CPLD EPM240 / EPM570 — сборник примеров (Verilog + BDF)

Репозиторий с учебными проектами для отладочных плат **EPM240 / EPM570 CPLD Development Board**.  
Все проекты проверены на реальном железе и максимально просты для понимания начинающими.

> Верхний уровень во многих примерах сделан в виде **BDF‑схемы** (Block Diagram File) в Quartus.
> Verilog‑модули подключаются к этой схеме как отдельные блоки.

---

## ⚙️ Оборудование и софт

- Платы: **EPM240 / EPM570 CPLD Development Board** (китайские учебные наборы).
- Питание: 5 В, логические уровни 3,3 В / 5 В — в зависимости от конкретного модуля.
- ПЗУ: CPLD EPM240T100 / EPM570T100 (в зависимости от версии платы).
- Среда разработки: **Intel/Altera Quartus II** (5.x–13.x, под старые CPLD).

Рекомендуется ставить ту же или близкую версию Quartus, что указана в исходных BDF‑проектах
(обычно 5.0–9.0).

---

## 🧱 Структура репозитория

В репозитории лежат отдельные демонстрационные проекты.  
Для каждого примера:

- один или несколько модулей **Verilog** (`*.v`),
- файл схемы верхнего уровня **BDF** (`*.bdf`) — подключает модули к выводам,
- файлы настроек Quartus (`*.qpf`, `*.qsf`),
- иногда — отдельный `readme` в папке проекта.

Ниже — краткое описание основных модулей.

---

## 🔦 Работа с кнопками и светодиодами

### `key_led.v`

- Прямое отображение состояния **кнопок** на **светодиоды**.
- Параметризуемый диапазон битов (`MSB_reg`, `LSB_reg`).
- Подходит как “Hello World” для проверки, что плата прошита и работает.

### `ckey_led.v`

- Отображение состояния **ползунковых переключателей** `ckey[1:4]` на светодиоды `led[1:4]`.
- Используется для демонстрации входов типа DIP‑/CKEY‑переключателей.
- Позволяет быстро проверить, что разъёмы и пины платы разведены правильно.

### `sled.v` (семисегментный индикатор — счётчик)

- Пример динамической индикации на 7‑сегментных индикаторах.
- По таймеру увеличивает счётчик и выводит его на все разряды индикатора.
- Показывает базовый принцип мультиплексирования разрядов.

### `led_water.v`

- Эффект “бегущей капли/воды” по 8 светодиодам.
- Последовательно зажигает каждый светодиод, создавая бегущий огонёк.
- Хороший пример работы с простым счётчиком и регистром.

### `beep_with_ckey_pins_pnp.v`

- Генерация звука на пьезо‑зуммер через **PNP‑транзистор**.
- Управление несколькими ползунковыми переключателями `ckey[1:4]`:
  - один включает/выключает звук,
  - остальные задают разные частоты/эффекты.
- В коде показан принцип деления частоты и управления выходом через транзистор.

---

## 🔢 Константы и индикация

### `constant.v` + семисегментные индикаторы

- Модуль `lpm_constant` (wizard‑генерация) выдаёт фиксированное значение.
- Это значение через другие модули подаётся на 7‑сегментный индикатор.
- Пример: как использовать IP‑модули Quartus и выводить просто “зашитые” числа.

---

## 📡 UART и обмен данными

### `my_uart_rx.v` / `my_uart_tx.v` / обвязка UART

- Приём и передача по **UART (RS‑232)**.
- Делитель частоты для формирования скорости передачи (bps).
- Пример: классический UART‑приёмник / передатчик на Verilog для CPLD.
- Используется в проекте “приём‑передача через COM‑порт” (loopback / обмен с ПК).

> На некоторых скоростях и тактовых частотах возможны проблемы со стабильностью —
> в репозитории оставлены “как есть” для учебных экспериментов.

### PS/2 → UART (клавиатура)

- Попытка принять коды PS/2‑клавиатуры и переслать их по UART.
- В README отмечено, что на конкретной тестовой клавиатуре проект работал нестабильно,
  поэтому используется больше как пример разбора протокола PS/2, чем готовое решение.

---

## 💡 Индикация на жидкокристаллических дисплеях

### `lcd.v` (LCD1602 “Hello World”)

- Работа с символьным дисплеем **LCD1602** (HD44780‑совместимый).
- Инициализация контроллера, вывод тестовой строки “Hello World”.
- Один из простейших примеров параллельного интерфейса с задержками и командами.

### `LCD12864.v`

- Работа с дисплеем **LCD12864** в текстовом режиме (HD44780‑совместимом).
- Инициализация и вывод 4 строк текста, например:

  ```text
  Our FPGA
  EDA NIOS II
  SOPC
  FPGA A
  ```

- Пример демонстрирует:
  - конфигурирование режима,
  - последовательность команд и задержек,
  - заполнение ОЗУ дисплея.

---

## 🎛 ЦАП TLC5615 — генерация напряжения

### `tlc5615.v` + обвязка

- Работа с 12‑битным ЦАП **TLC5615** по последовательному интерфейсу.
- На вход подаётся цифровой код, который преобразуется в аналоговое напряжение.
- В примере код задаётся от ползунков / кнопок, можно наблюдать изменение уровня на выходе ЦАП.
- Есть вариант “китайской” прошивки, где ЦАП просто даёт 5 В без регулировки —
  здесь реализован **полный диапазон от 0 до опорного напряжения**.

> На практике вы получаете цифровое управление напряжением: меняем код → меняется выход ЦАП.

---

## 📥 АЦП TLC549 — измерение напряжения

### `adc.v` (TLC549)

- Контроллер для 8‑битного АЦП **TLC549**.
- Функции:
  - формирование такта `adc_clk`,
  - управление `cs_n` (выбор кристалла),
  - приём 8‑битного последовательного слова по `sdat_in`,
  - выдача результата в `data_out[7:0]`,
  - импульс `data_ready` при появлении новых данных.
- Внутри реализован конечный автомат:

  ```text
  idle → adc_ready → adc_receive → adc_conversion → adc_data_load → idle
  ```

- В данной плате опорное напряжение TLC549 формируется через **TL431 ≈ 2.5 В**,
  поэтому *полный диапазон* АЦП — **0…2.5 В = коды 00…FF**.
  Всё, что выше ~2.5 В, приводит к насыщению и выдаче кода `FF`.

---

## 🔢 Динамическая индикация результата АЦП

### `scan_led.v`

- Универсальный сканер для **8‑разрядного 7‑сегментного индикатора**.
- На вход `d[31:0]` подаётся число (8 шестнадцатеричных цифр),
  модуль поочерёдно выбирает каждый разряд и выводит соответствующую цифру.
- Частота сканирования задаётся входом `clk_1k` (обычно ~1 кГц).
- Используется для отображения:
  - значения АЦП (TLC549),
  - счётчиков и констант,
  - отладочной информации.

Пример: вывести байт АЦП в виде двух HEX‑цифр, остальные разряды — нули.

```verilog
wire [7:0]  adc_data;
wire [31:0] disp_bus;

assign disp_bus = {24'h000000, adc_data}; // справа два hex‑разряда

scan_led u_scan (
    .clk_1k (clk_1k),
    .d      (disp_bus),
    .dig    (dig),
    .seg    (seg)
);
```

---

## ⏱ Делитель частоты

### `int_div.v`

- Универсальный **делитель частоты** с параметром `F_DIV`.
- Вход: `clock` (системный такт, например 50 МГц).
- Выход: `clk_out = clock / F_DIV` (примерно, с максимально ровной скважностью).
- При чётном `F_DIV` скважность близка к 50%.
- При нечётном `F_DIV` используются два счётчика по фронту и спаду для улучшения симметрии.

Используется для:

- формирования ~1 кГц для `scan_led`,
- генерации нужной частоты для UART,
- любых внутренних таймеров/интервалов.

---

## 🧩 BDF — верхний уровень проектов

Во многих примерах верхний уровень выполнен в виде **BDF‑файла**:

- на схеме размещаются блоки‑модули (`key_led`, `scan_led`, `adc`, `int_div` и т.п.),
- выводы схемы привязываются к пинам микросхемы CPLD (через Assignment Editor),
- удобно визуально видеть, как соединены кнопки, светодиоды, индикаторы, разъёмы.

Verilog‑модули из этого репозитория подключаются к BDF как **иконки‑блоки**.

---

## ▶️ Видео и демонстрации

Для многих проектов записаны видео‑демонстрации на YouTube  
(подключение к плате, поведение индикаторов, объяснение кода).

Плейлист FPGA / CPLD:
- `https://www.youtube.com/playlist?list=PLVoFIRfTAAI7-d_Yk6bNVnj4atUdMxvT5`

---

## 📎 Как использовать проекты

1. Открыть нужный проект в Quartus (`*.qpf`).
2. Убедиться, что выбран правильный чип (EPM240 / EPM570).
3. Проверить/при необходимости скорректировать назначения выводов (`*.qsf` / Pin Planner).
4. Скомпилировать проект.
5. Зашить прошивку в плату через USB‑Blaster или аналогичный программатор.
6. Повторять и экспериментировать с кодом.

Этот репозиторий задуман как **“живой справочник” для новичка по CPLD/FPGA** на примерах:
кнопки, светодиоды, семисегментники, UART, LCD, ЦАП, АЦП и т.д.
